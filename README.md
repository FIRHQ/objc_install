
# objc_install Step
Install dependencies use `pod install`

### INPUTS

## EXAMPLE 

```yml
steps:
  - name: objc_install
    enable: true
    failure: true
    plugin:
      name: objc_install
      inputs:
```
